const kThemeCacheKey = 'theme.mode';

const kTokenCacheKey = 'token';

const kRefreshTokenCacheKey = 'refresh_token';

const kPasswordLessTokenCacheKey = 'password_less_token';

const kPasswordLessPinCacheKey = 'password_less_pin';

const kSelectedLocaleCacheKey = 'selected_locale';
